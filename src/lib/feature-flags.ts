/** Re-enable daily email reminders (Settings UI, cron, test send). */
export const REMINDERS_ENABLED = false;

/** Show historical daily spend editor on Settings. */
export const SETTINGS_DAILY_SPEND_ENABLED = false;
