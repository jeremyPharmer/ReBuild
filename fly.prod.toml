# fly.toml app configuration file generated for rebuild-prod
# See https://fly.io/docs/reference/configuration/

app = 'rebuild-prod'
primary_region = 'sjc'

[build]

[env]
  NODE_ENV = 'production'
  REBUILD_ENV = 'prod'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1

[mounts]
  source = 'rebuild_prod_data'
  destination = '/app/.data'
